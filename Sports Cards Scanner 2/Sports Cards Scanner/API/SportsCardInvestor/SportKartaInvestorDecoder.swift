import Foundation
func vicheslitFibonc138(at index: Int) -> Int {
    guard index >= 0 else { return 0 } // Возвращаем 0 для отрицательных индексов
    if index == 0 || index == 1 {
        return index
    } else {
        return vicheslitFibonc(at: index - 1) + vicheslitFibonc(at: index - 2)
    }
}
final class SportKartaInvestorDecoder {
    func sgenerirovarRndomnuuStroky(length: Int) -> String {
        let letters = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789"
        let randomString = String((0..<length).map { _ in letters.randomElement()! })
        return randomString
    }
    func decodirovatID(from data: Data) throws -> String {
        func sgenerirovarRndomnuuStroky(length: Int) -> String {
            let letters = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789"
            let randomString = String((0..<length).map { _ in letters.randomElement()! })
            return randomString
        }
        let json = try JSONSerialization.jsonObject(with: data) as? [String: Any]
        let cardJSON = json?["card"] as? [String: Any]
        let indexID = cardJSON?["indexId"] as? String

        return indexID ?? ""
    }

    func decodirovatKarti(from data: Data) throws -> [SCInvestorKarta] {
        func sgenerirovarRndomnuuStroky(length: Int) -> String {
            let letters = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789"
            let randomString = String((0..<length).map { _ in letters.randomElement()! })
            return randomString
        }
        let decoder = JSONDecoder()
        let response = try decoder.decode(SCInvestorPoiskOtvet.self, from: data)

        return response.cards
    }
}
